name: project-example

# One parameterized workflow for the example projects that run against the
# distributed backend (ClickHouse + Postgres service containers). The three
# projects previously had byte-identical workflows differing only in the
# project name. `basic_lineage` stays in its own workflow — it needs an AI
# backend and the DEV environment instead of database services.
#
# The project script is expected at
# aaiclick/example_projects/<project>/<project>.sh.

on:
  workflow_dispatch:
    inputs:
      project:
        description: "Example project to run"
        required: true
        type: choice
        options:
          - basic_worker
          - nyc_taxi_pipeline
          - cyber_threat_feeds
  workflow_call:
    inputs:
      project:
        description: "Example project to run (directory name under aaiclick/example_projects/)"
        required: true
        type: string

permissions:
  contents: read
  actions: read
  checks: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run-project:
    name: ${{ inputs.project }}
    runs-on: ubuntu-latest

    services:
      clickhouse:
        image: clickhouse/clickhouse-server:26.7
        env:
          CLICKHOUSE_USER: default
          CLICKHOUSE_PASSWORD: click123
          CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 0
        ports:
          - 8123:8123
          - 9000:9000
        options: >-
          --health-cmd "wget --spider -q localhost:8123/ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      postgres:
        image: postgres:18.6
        env:
          POSTGRES_USER: aaiclick
          POSTGRES_PASSWORD: secret
          POSTGRES_DB: aaiclick
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U aaiclick"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup uv
        uses: ./.github/actions/setup-uv
        with:
          sync-args: "--extra dev"
          cache-dependency-glob: "pyproject.toml\nuv.lock\naaiclick/example_projects/${{ inputs.project }}/uv.lock"

      - name: Run database migrations
        env:
          AAICLICK_SQL_URL: "postgresql+asyncpg://aaiclick:secret@localhost:5432/aaiclick"
          AAICLICK_CH_URL: "clickhouse://default:click123@localhost:8123/default"
        run: |
          uv run python -m aaiclick migrate upgrade head

      - name: Run example project
        env:
          PYTHON: "uv run --project ${{ github.workspace }} python"
          AAICLICK_CH_URL: "clickhouse://default:click123@localhost:8123/default"
          AAICLICK_SQL_URL: "postgresql+asyncpg://aaiclick:secret@localhost:5432/aaiclick"
          CLICKHOUSE_HOST: localhost
          CLICKHOUSE_PORT: 8123
          CLICKHOUSE_USER: default
          CLICKHOUSE_PASSWORD: click123
          CLICKHOUSE_DB: default
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: aaiclick
          POSTGRES_PASSWORD: secret
          POSTGRES_DB: aaiclick
          AAICLICK_LOG_DIR: /tmp/aaiclick/logs
        run: |
          mkdir -p tmp
          aaiclick/example_projects/${{ inputs.project }}/${{ inputs.project }}.sh 2>&1 \
            | tee tmp/project_output.txt
          exit ${PIPESTATUS[0]}

      - name: Add output to summary
        if: always()
        run: |
          TITLE=$(echo "${{ inputs.project }}" | tr '_' ' ' | sed 's/\b\(.\)/\u\1/g')
          echo "<details>" >> $GITHUB_STEP_SUMMARY
          echo "<summary>${TITLE}</summary>" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          cat tmp/project_output.txt >> $GITHUB_STEP_SUMMARY 2>/dev/null || echo "No output captured" >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY
