# Examples Guidelines

## Project Structure

- Each example project should have a `report.py` file containing all report printout logic
- The `@job` function returns the report task, which has all other tasks as dependencies
