[project]
name = "basic-lineage"
version = "0.1.0"
description = "AI-powered lineage explanation and debugging for a revenue pipeline"
requires-python = ">=3.10"

dependencies = [
    "aaiclick[ai]",
]
