[project]
name = "cyber-threat-feeds"
version = "0.1.0"
description = "Multi-source cybersecurity pipeline with CISA KEV, Shodan CVEDB, and FIRST EPSS"
requires-python = ">=3.10"

dependencies = [
    "aaiclick",
]
